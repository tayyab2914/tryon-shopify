---
name: support-email
description: Where the app's support/contact email is configured and its current value
metadata:
  type: project
---

The support/contact email is centralized in `app/lib/links.ts` as `SUPPORT_EMAIL`, used by the public `/privacy` page, the embedded admin Help card, the landing footer, the Custom-plan contact link, and the App Store listing.

Current value: `abdullahkhn8902@gmail.com` (the developer account email — a working placeholder). Should be replaced with a branded support address before final App Store submission. See [[billing-model]].
